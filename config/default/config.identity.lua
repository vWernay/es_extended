Config.Modules.identity = {
  -- in seconds, represent the time between each player identity coordinates saves
  playerCoordsSaveInterval = 10,
  -- in seconds, represent the time between each player death check
  isPlayerDeadCheckInterval = 0.1,
}